// Override the method you create in Animal class

    // get getAge() { return "the pet age is " + this.age }
    // got overwrited by getInfo() {return this.identifier + " is a dog! "} in dog class
    // got overwrited by getInfo() {return this.identifier + " is a cat! "} in cat class